[pytest]
testpaths = tests
python_files = test_*.py

# ==================== FIXED & CLEANED ====================
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
    ignore::pytest.PytestDeprecationWarning